# Default couple avatars

Drop the two cartoon avatar images here (served at `/avatars/...`):

- `boy.png`  — the boy avatar
- `girl.png` — the girl avatar

They are used by `pf-couple-avatar` (home / todos / us / wishlist headers). If the
files are missing the UI falls back to a 💑 emoji.
